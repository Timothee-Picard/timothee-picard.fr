{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "static": "nuxt generate && nuxt start",
    "start": "node .output/server/index.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.14",
    "nuxt-icon": "^0.1.7",
    "sass": "^1.56.1",
    "sass-loader": "^10.4.1"
  }
}
